apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  # Add new resources here as the course progresses:
  # Week 5: configmap.yaml, secret.yaml, redis-deployment.yaml, etc.
  # Week 6: ingress.yaml, networkpolicy.yaml
  # Week 7: (replaced by Helm chart or Kustomize overlays)
